{% extends 'AdminBundle::layout.html.twig' %}
{% block header %}
    
{% endblock header %}
{% block body -%}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Thực Đơn</h1>
            </div>
            <div class=".col-lg-1">
                <button type="button" class="btn btn-success">Thêm Thực Đơn</button>
            </div>
            <br/>
            <!-- /.col-lg-12 -->
        </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-primary">
                <div class="panel-heading"> <i class="glyphicon glyphicon-menu-hamburger"></i> Thực Đơn </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="dataTable_wrapper">


                            <table class="table table-striped table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th>STT</th>
                                        <th>Tên</th>
                                        <th>Chi Tiếc</th>
                                        <th>Hình Ảnh</th>
                                        <th>Giá</th>
                                        <th>Ghi Chú</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% set num = numberRecord * (page - 1) %}
                                    {% for entity in entities %}
                                        <tr {% if num != 1 %}
                                            style="color: #A7A7A7;"
                                            {% endif %}
                                                >
                                                {% set num = num + 1 %}
                                                <td>{{ num }}</td>
                                                <td>{{ entity.name }}</td>
                                                <td>{{ entity.detail }}</td>
                                                <td>
                                                    <img src="{{ asset(entity.image) }}" class="img-thumbnail"/>
                                                </td>
                                                <td>{{ entity.gia }}</td>
                                                <td>{{ entity.note }}</td>
                                                <td>
                                                    <a href="#" class="btn btn-primarybtn btn-info">Edit</a>
                                                </td>
                                            </tr>
                                            {% endfor %}
                                            </tbody>
                                        </table>
                                        <div class="navigation">
                                            {{ knp_pagination_render(entities) }}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
                {% endblock %}

